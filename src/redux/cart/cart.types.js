//Action types are exported as a variable so that it if it needs to change it only needs to be changed in one place
const CartActionTypes = {
  TOGGLE_CART_HIDDEN: 'TOGGLE_CART_HIDDEN',
  ADD_ITEM: 'ADD_ITEM'
};

export default CartActionTypes;
